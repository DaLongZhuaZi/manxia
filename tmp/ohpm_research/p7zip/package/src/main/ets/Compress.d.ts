export declare class Compress {
    static compressToPath(h: string, i: string): Promise<void>;
    static compressToPathEncrypted(e: string, f: string, g: string): Promise<void>;
    static compressWithPreset(a: string, b: string, c: string, d: number): Promise<void>;
}
